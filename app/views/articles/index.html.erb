<h1>Articles</h1>
<p>This blog has <%= Article.public_count %> articles and <%= Comment.public_count %> comments.</p>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>
<%= link_to "New article", new_article_path, class: "btn btn-primary" %>
